"""User accounts and authentication.

fastapi-users is wrapped behind this package (models, manager, backend) so the
rest of the application only imports from ``app.users`` — the same resilience
seam we keep around blinkpy.
"""
